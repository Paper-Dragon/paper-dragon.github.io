import{_ as s}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as a,c as n,e}from"./app-zQGmJfuc.js";const i="/assets/image-20230706154345300-dAh9cA1b.png",l="/assets/image-20230706155010020-bFcqg_RD.png",t={},d=e(`<h1 id="ssh日志记录登陆密码" tabindex="-1"><a class="header-anchor" href="#ssh日志记录登陆密码" aria-hidden="true">#</a> ssh日志记录登陆密码</h1><h1 id="ssh记录登陆密码" tabindex="-1"><a class="header-anchor" href="#ssh记录登陆密码" aria-hidden="true">#</a> ssh记录登陆密码</h1><p>了解到ssh自身不记录登陆的用户名密码</p><p>但我们可以通过修改ssh源码或者打补丁的方式来实现对远程登陆用户密码的记录</p><p>只需修改ssh源码或者打补丁重新编译，安装</p><p>即可获得一个新的ssh用户端</p><h2 id="过程" tabindex="-1"><a class="header-anchor" href="#过程" aria-hidden="true">#</a> 过程</h2><h3 id="_1、准备环境" tabindex="-1"><a class="header-anchor" href="#_1、准备环境" aria-hidden="true">#</a> 1、准备环境</h3><p>安装对应的openssh源码包</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">wget</span> https://mirror.leaseweb.com/pub/OpenBSD/OpenSSH/portable/openssh-7.5p1.tar.gz


<span class="token comment"># 各个版本源码下载</span>
<span class="token comment"># https://mirror.leaseweb.com/pub/OpenBSD/OpenSSH/portable/</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>创建相应的安装配置路径</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> <span class="token parameter variable">-p</span> /opt/ssh7.5p1/conf
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>当然还要安装相应依赖，这一步关乎后面的编译是否成功</p><p>注意debian和centos的依赖包名还不一样</p><p>#centos</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>yum <span class="token function">install</span> gcc zlib-devel openssl-devel pam-devel pam-krb5 krb5-devel krb5-libs <span class="token parameter variable">-y</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>#debian</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">apt-get</span> <span class="token function">install</span> gcc zlib1g-dev  libssl1.0-dev <span class="token parameter variable">-y</span>  <span class="token comment"># centos指令已经优化，这个没有</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_2、修改源码" tabindex="-1"><a class="header-anchor" href="#_2、修改源码" aria-hidden="true">#</a> 2、修改源码</h3><p>解压刚下载的源码包</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tar</span> <span class="token parameter variable">-zxvf</span> openssh-7.5p1.tar.gz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>进入目录</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> openssh-7.5p1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>对源码进行一些修改</p><p>增加在日志中添加登陆用户名密码的功能</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">vim</span> auth-passwd.c
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>添加一段代码</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>#找到函数auth_password，添加
<span class="token function">logit</span><span class="token punctuation">(</span><span class="token string">&quot;username: %s password: %s&quot;</span><span class="token punctuation">,</span> authctxt<span class="token operator">-&gt;</span>user<span class="token punctuation">,</span> password<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><figure><img src="`+i+`" alt="image-20230706154345300" tabindex="0" loading="lazy"><figcaption>image-20230706154345300</figcaption></figure><h3 id="_3、编译安装" tabindex="-1"><a class="header-anchor" href="#_3、编译安装" aria-hidden="true">#</a> 3、编译安装</h3><p>开始编译</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>./configure <span class="token parameter variable">--prefix</span><span class="token operator">=</span>/usr <span class="token punctuation">\\</span>
	<span class="token parameter variable">--sysconfdir</span><span class="token operator">=</span>/etc/ssh <span class="token punctuation">\\</span>
	--with-pam --with-zlib <span class="token punctuation">\\</span>
	--with-ssl-dir<span class="token operator">=</span>/usr <span class="token punctuation">\\</span>
	--with-md5-passwords <span class="token punctuation">\\</span>
	<span class="token parameter variable">--mandir</span><span class="token operator">=</span>/usr/share/man <span class="token punctuation">\\</span>
	--with-kerberos5<span class="token operator">=</span>/usr/lib64/libkrb5.so
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><em>#参数说明</em></p><p>--prefix=/usr \\ #安装目录 --sysconfdir=/etc/ssh \\ #配置文件目录 --with-pam --with-zlib \\ # PAM headers not found --with-ssl-dir=/usr <br> --with-md5-passwords <br> --mandir=/usr/share/man <br> --with-kerberos5=/usr/lib64/libkrb5.so # (防止出现Unsupported option GSSAPIAuthentication 和 Unsupported option GSSAPICleanupCredentials)</p></blockquote><p>在没有报错的情况下</p><p>进一步</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">make</span>
<span class="token function">make</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>之后就可以启动使用</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>/opt/ssh7.5p1/sbin/sshd
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>默认22端口，记得先把原先ssh停掉</p><p><strong>注：ssh升级后，默认关闭root远程登录，如果需要，请打开5、如果无法登录，请修改/etc/pam.d/sshd</strong></p><h3 id="_4、登陆配置修改" tabindex="-1"><a class="header-anchor" href="#_4、登陆配置修改" aria-hidden="true">#</a> 4、登陆配置修改</h3><p>按照前面设定的配置目录找到sshd_config</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment">#修改配置文件</span>
<span class="token function">vim</span> /opt/ssh7.5p1/conf/sshd_config
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>就跟正常改ssh配置文件操作一样，像允许远程用户登陆、修改端口</p><div class="language-perl line-numbers-mode" data-ext="perl"><pre class="language-perl"><code><span class="token comment">#修改端口</span>
Port <span class="token number">1022</span>
<span class="token comment">#允许root账号登陆</span>
PermitRootLogin yes
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_5、日志查看" tabindex="-1"><a class="header-anchor" href="#_5、日志查看" aria-hidden="true">#</a> 5、日志查看</h3><p>centos和debain日志位置不同</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment">#centos</span>
<span class="token function">grep</span> <span class="token string">&#39;username:&#39;</span> /var/log/messages
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment">#debian</span>
<span class="token function">grep</span> <span class="token string">&#39;username:&#39;</span> /var/log/auth.log
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>远程登陆一波</p><p>运行结果如下：</p><figure><img src="`+l+`" alt="image-20230706155010020" tabindex="0" loading="lazy"><figcaption>image-20230706155010020</figcaption></figure><h1 id="systemctl命令无法管理sshd服务问题解决" tabindex="-1"><a class="header-anchor" href="#systemctl命令无法管理sshd服务问题解决" aria-hidden="true">#</a> systemctl命令无法管理sshd服务问题解决</h1><h2 id="解决方法" tabindex="-1"><a class="header-anchor" href="#解决方法" aria-hidden="true">#</a> 解决方法</h2><p>以下是我当前情况的解决方案，不代表所有类似情况都适用。</p><p>将源码安装包中 contrib/redhat/sshd.init 文件复制到 /etc/init.d/ 目录下并添加可执行权限。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cp</span> contrib/redhat/sshd.init /etc/init.d/
<span class="token function">chmod</span> +x /etc/init.d/sshd.init
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>然后,使用该脚本启动sshd服务</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>/etc/init.d/sshd.init start
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这时，/run/systemd/generator.late/ 目录下会产生一个名为 sshd.service的 systemd 服务配置文件。 将这个文件复制到 systemd 的服务配置文件目录下</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cp</span> /run/systemd/generator.late/sshd.service  /usr/lib/systemd/system/sshd.service
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这时再通过systemctl命令操作sshd服务就正常了。 查看该自动生成的service配置文件设置可见，对应的启动、停止等命令都已经换成了对 sshd.init 脚本的操作，而不是原来的对 /usr/sbin/sshd 命令的操作。 文件内容如下：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Automatically generated by systemd-sysv-generator</span>

<span class="token punctuation">[</span>Unit<span class="token punctuation">]</span>
<span class="token assign-left variable">Documentation</span><span class="token operator">=</span>man:systemd-sysv-generator<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>
<span class="token assign-left variable">SourcePath</span><span class="token operator">=</span>/etc/rc.d/init.d/sshd.init
<span class="token assign-left variable">Description</span><span class="token operator">=</span>SYSV: OpenSSH server daemon

<span class="token punctuation">[</span>Service<span class="token punctuation">]</span>
<span class="token assign-left variable">Type</span><span class="token operator">=</span>forking
<span class="token assign-left variable">Restart</span><span class="token operator">=</span>no
<span class="token assign-left variable">TimeoutSec</span><span class="token operator">=</span>5min
<span class="token assign-left variable">IgnoreSIGPIPE</span><span class="token operator">=</span>no
<span class="token assign-left variable">KillMode</span><span class="token operator">=</span>process
<span class="token assign-left variable">GuessMainPID</span><span class="token operator">=</span>no
<span class="token assign-left variable">RemainAfterExit</span><span class="token operator">=</span>no
<span class="token assign-left variable">PIDFile</span><span class="token operator">=</span>/var/run/sshd.pid
<span class="token assign-left variable">ExecStart</span><span class="token operator">=</span>/etc/rc.d/init.d/sshd.init start
<span class="token assign-left variable">ExecStop</span><span class="token operator">=</span>/etc/rc.d/init.d/sshd.init stop
<span class="token assign-left variable">ExecReload</span><span class="token operator">=</span>/etc/rc.d/init.d/sshd.init reload
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,63),r=[d];function p(c,o){return a(),n("div",null,r)}const h=s(t,[["render",p],["__file","ssh日志记录登陆密码.html.vue"]]);export{h as default};
